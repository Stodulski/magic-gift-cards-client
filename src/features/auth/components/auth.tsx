import { AuthForm } from './authForm'

export const Auth = () => {
  return (
    <main className='w-full bg-gray-100 h-[100vh] flex justify-center items-center bg'>
      <AuthForm />
    </main>
  )
}
